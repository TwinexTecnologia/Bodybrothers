import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.twinex.bodybrothers.student',
  appName: 'FitBody Student',
  webDir: 'dist'
};

export default config;
